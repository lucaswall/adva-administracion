{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["npm ci --include=dev"]
    },
    "build": {
      "commands": ["npm run build", "npm prune --omit=dev"]
    }
  }
}

/**
 * Webhook routes for external notifications
 */
import type { FastifyInstance } from 'fastify';
/**
 * Register webhook routes
 */
export declare function webhookRoutes(server: FastifyInstance): Promise<void>;

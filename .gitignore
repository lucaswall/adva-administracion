
_samples
_samples*

# Dependencies
node_modules
node_modules/

# Build output
dist/

# Cache
.cache/

# Environment variables (contains secrets)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/

# Service account keys (never commit these!)
service-account.json
*-service-account.json
*.pem

# Apps Script generated files (config.ts is regenerated from config.template.ts on every build)
apps-script/src/config.ts

# Claude Code local settings (personal overrides)
.claude/settings.local.json
.claude/scheduled_tasks.lock
# Agent memories — never commit. Wildcard so subprojects (e.g. mcp-gemini/.claude/) are covered too.
**/.claude/agent-memory/
